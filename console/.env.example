# Catalog base URL (injected at build time). Use for local dev or when pointing at a k8s service.
# Copy to .env or .env.local and set your value, or run: PATTERN_UI_CATALOG_BASE_URL=http://localhost:8080 yarn start
PATTERN_UI_CATALOG_BASE_URL=
OPERATOR_NAMESPACE=